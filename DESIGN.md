# Design: PA Skateparks Directory Rebuild

Generated by /office-hours on 2026-04-25
Branch: unknown
Repo: paskateparks
Status: APPROVED (Revised 2026-05-30 — read Handoff Briefing below)
Mode: Builder

> **⚠️ ARCHITECTURE PIVOT 2026-05-30:** The Airtable-related architecture in this doc (mentions of `AIRTABLE_API_KEY`, Airtable record ceilings, Airtable as the CMS, the schema in §Next Steps step #2) is **superseded by [STACK-PIVOT.md](STACK-PIVOT.md)**. The product decisions (D1–D30) are still valid; the data + hosting layer pivoted from Airtable to Supabase + Drizzle + Vercel for cost ($0/yr vs $240/yr) and simplicity (~half the architectural surface area). Read STACK-PIVOT.md as source of truth for anything about the database, file storage, image hosting, CMS admin, or deployment pipeline.

---

## Handoff Briefing (read this first)

**Project.** PA Skateparks Directory Rebuild — replacing paskateparks.com (WordPress) with Next.js + Airtable + Vercel + Leaflet. Parent-first directory of 150+ Pennsylvania skateparks, with a conditional Programming module that funnels parents to the owner's coaching business.

**Status (as of 2026-05-30).** Product design locked across 30 decisions (D1–D30). Schema is complete: Parks, Builders, Shops, Suggestions, Photos tables. Visual design system deferred to a separate `/design-consultation` session. Rating system explicitly parked for v1. Ready for architecture review (`/plan-eng-review`).

### Reading order

1. **You're reading the right thing.** Start with this briefing.
2. **Then read the Revisions section below.** It is the source of truth where it conflicts with the original doc.
3. **Original sections** (Problem Statement onwards) are historical context — useful for understanding intent but superseded by Revisions on specifics.
4. **Read the companion docs** (table below) before running `/plan-eng-review`. Especially SITE-AUDIT.md.

### Companion docs in this project folder

| File | What it contains |
|---|---|
| [SITE-AUDIT.md](SITE-AUDIT.md) | Full audit of the live paskateparks.com — tech stack, URL structure, data model, content inventory, migration risks. **Required reading before architecture review.** |
| [mobile-design/](mobile-design/) | 7 mobile mockup screenshots of a park profile page. Visual reference for D9–D30. The Revisions section references "the mockup" throughout — this is it. |
| [park-urls.txt](park-urls.txt) | All 47 live `/park/<slug>/` URLs that must be preserved verbatim in the rebuild (SEO equity). |
| [audit-screenshots/](audit-screenshots/) | Screenshots from the live site audit — homepage, /parks/, /all-parks/, /map/, two park profiles, builder page, shop page, style guide placeholder. |

### What `/plan-eng-review` should focus on

This design doc locks **what** to build. Architecture is what's open. Eng review should produce decisions on:

- **Data import pipeline** — WordPress (3 custom post types `park` / `builder` / `shop`, 3 custom taxonomies) → Airtable. One-time bulk import + ongoing content authoring for the 99 stub parks. Lat/lng extraction specifically requires WP DB export, not HTML scraping (see SITE-AUDIT.md §9 #2).
- **Image hosting** — current photos live at `paskateparks.com/wp-content/uploads/`. Migrate to where? Airtable attachments (storage limits)? Cloudinary? S3 + CDN? Phased migration with WP as transitional source?
- **Airtable → Next.js build pipeline** — ISR with `revalidatePath` triggered by a Vercel deploy hook (per original §Distribution Plan). Confirm Airtable rate-limit handling for the single paginated list call at build time.
- **Build-time compute** — Haversine for Nearby Parks (3 closest within 30mi) and Nearby Shops (same, no state border rule per D7). Per-park computed values stored in page props.
- **Suggestions backend** — D28 modal on park profile → Airtable Suggestions table. Anti-spam strategy (rate limit, honeypot, captcha)? Submission auth (anonymous OK)? Moderation workflow.
- **410 Gone routing** — 14 `/builder/<slug>/` + 20 `/shop/<slug>/` URLs need HTTP 410 in `next.config.js` (per D3). Confirm Next.js routing approach.
- **Status banner UX** — D11: Permanently Closed parks keep their pages live but should communicate closure prominently. Banner spec, dismissibility, visual weight.
- **Photos child table query** — D29: Photos is a child table linked to Parks. Confirm Airtable linked-record fetch + Order-based sorting at build time.
- **Programming module** — D27: conditional render mid-page when `Programming = true`. CTAs are template-level, not per-park. Confirm template handles the toggle cleanly without layout shift.
- **CI/CD** — Vercel GitHub integration auto-deploy on merge. Airtable webhook → Vercel deploy hook on record save. Confirm.
- **Performance budget** — mockup is mobile-first; original premise is "parent in a parking lot on an Android." Target: TTI under 3s on 4G, LCP under 2.5s.

### Open questions going into eng review (operational, non-blocking)

1. **Lat/lng extraction** — WP Google Maps plugin tables; need DB export, not HTML scraping (SITE-AUDIT.md §9 #2)
2. **County coverage** — only 14 of 67 PA counties have parks; affects "statewide" marketing claim, not schema (SITE-AUDIT.md §9 #4)
3. **`/new-park/` form destination** — where do existing submissions go today? Preserve the flow into the new Suggestions table (SITE-AUDIT.md §9 #6)

### Deferred to future iterations

- **Park rating system (1–10 score)** — full architecture analysis in Revisions § "Deferred to future iterations"
- **Visual design system** — to be defined via `/design-consultation` in a separate session
- **Blog / Events content** — out of scope for v1 (SITE-AUDIT.md §9 #5)
- **Embeddable widget** — confirmed deferred in Revisions

---

## Revisions

### Decisions index (D1–D30)

Single reference table for all 30 locked decisions. Detailed rationale and tradeoffs are in the dated subsections below.

| # | Topic | Decision | Source |
|---|---|---|---|
| D1 | Builders + Shops surface area | On park profiles only, outbound links in context | Post site audit |
| D2 | Outbound link policy | Owner curates single best link per business (web or active social) | Post site audit |
| D3 | Existing `/builder/` + `/shop/` URLs (34 total) | 410 Gone in `next.config.js` | Post site audit |
| D4 | Builders attribution | Yes on profile, multi-builder supported | Post site audit |
| D5 | Nearby Shops | Top 3 within 30 miles, computed at build time | Post site audit |
| D6 | Empty link state | Render as plain text (name only) | Post site audit |
| D7 | Shop geographic boundary | Anywhere within 30 miles (no state border rule) | Post site audit |
| D8 | Optional logos | Both Builders and Shops, side-by-side with name | Post site audit |
| D9 | Hero contents | Photo, city/state, name, established year only | Mockup walkthrough |
| D10 | Park Type / Sqft / County | Dropped from public profile (filter facets only) | Mockup walkthrough |
| D11 | Status surfacing | Silent when Open, banner when Closed (page stays live) | Mockup walkthrough |
| D12 | Address shape | Structured: Street + City + State + ZIP + Lat + Lng | Mockup walkthrough |
| D13 | Hours | Single long-text field, line-break stanzas | Mockup walkthrough |
| D14 | Overview photos | Single Photos field, horizontally scrollable strip | Mockup walkthrough |
| D15 | Vehicle types | 4 checkboxes + `VehicleRulesNotes` | Mockup walkthrough |
| D16 | Pads / Helmets | `Helmets` single-select + `OtherPadsRequired` boolean | Mockup walkthrough |
| D17 | Fee | Single boolean, no cost detail field | Mockup walkthrough |
| D18 | Amenity model | Universal Y/N + Notes + Photo for all 7 rows | Mockup walkthrough |
| D19 | Amenity typology | Dropped entirely — typology goes in Notes | Mockup walkthrough |
| D20 | Riding Surface | Multi-select + Notes + Photo | Mockup walkthrough |
| D21 | Connect link shape | Single multi-line `ParkLinks` field, `Type \| URL` per line | Mockup walkthrough |
| D22 | Operator | Not imported at all | Mockup walkthrough |
| D23 | Support links | Folded into `ParkLinks`, renderer segments by Type | Mockup walkthrough |
| D24 | Nearby Parks | Kept, 3 closest within 30mi, same visual as Nearby Shops | Mockup walkthrough |
| D25 | Obstacles | Chip/tag list on profile (multi-select, 38 options) | Mockup walkthrough |
| D26 | Builders placement | Small standalone section near bottom, before Nearby Parks | Mockup walkthrough |
| D27 | Programming module (renamed from Coaching) | Mid-page section, renders when `Programming = true` | Mockup walkthrough |
| D28 | Suggest an Edit | Modal on park page → Airtable Suggestions table | Mockup walkthrough |
| D29 | Photo credits | `Photos` becomes a child table (ParkRef + File + Credit + Caption + Order) | Resolved 2026-05-30 |
| D30 | Renovated dates | Multi-line `RenovatedYears` text field, optional `YYYY: notes` per line | Session 2 close |

### 2026-05-30 — Session 2 (post site audit)

After the live site audit ([SITE-AUDIT.md](SITE-AUDIT.md)), the following decisions extend or refine this design. Original sections below remain as historical context; this section is the current source of truth where it conflicts.

#### Park scope clarification

**All 150+ parks will have their own profile pages.** The 47-of-146 ratio on the live site reflects unfinished content authoring, not a tiered strategy. The rebuild template must render gracefully when fields are missing (description, photos, obstacles, etc.) — data will be filled in over time. Same graceful-degradation rule as the builder/shop sections: hide empty sections silently, render available data, never show placeholder copy like "coming soon."

#### Builders + Shops — 8 decisions locked

1. **No standalone pages.** Builders and shops do not get their own URLs. They appear only as contextual sections on park profiles.
2. **Single curated outbound link per business.** Owner picks the best link — official website or active social (Instagram/Facebook). Inactive social accounts excluded by owner judgment.
3. **Existing 34 URLs → 410 Gone.** All `/builder/<slug>/` and `/shop/<slug>/` URLs return HTTP 410 in `next.config.js`. Tells Google these are permanently deleted. No redirect maintenance.
4. **Builders section on park profile — multi-builder supported.** Renders as comma-separated outbound credits ("Built by Spohn Ranch Skateparks → 5th Pocket Skateparks →"). Joint builds get full attribution.
5. **Nearby Shops on park profile — top 3 within 30 miles.** Build-time Haversine computation against shop lat/lng. Section hides silently if 0 in range. **No state border rule** — Cleveland, Trenton, etc. eligible if they're the closest.
6. **Empty link state = plain text.** Records with name but no curated link show as text only. Same rule for builders and shops. Handles DIY ("Built by DIY"), new shops awaiting curation.
7. **Optional logos for both builders and shops.** Logo + name side by side inside one outbound link. No logo → name only. Same accessible fallback regardless.
8. **Forward-looking:** Shops on map — `Lat`/`Lng` required from day 1 so the feature can flip on without schema migration. Builders on map — not planned (orgs, not locations).

#### Schema additions

**Builders table** (~14 records)

| Field | Type | Required |
|---|---|---|
| Name | Single line text | ✅ |
| Link | URL | — |
| Logo | Attachments (1) | — |

**Shops table** (~20+ records — PA + border-state shops within 30 miles of a PA park)

| Field | Type | Required |
|---|---|---|
| Name | Single line text | ✅ |
| Link | URL | — |
| Logo | Attachments (1) | — |
| Address | Single line text | — |
| Lat | Number | ✅ |
| Lng | Number | ✅ |
| State | Single select (PA, NJ, NY, OH, WV, MD, DE) | — |

**Parks table additions** (extends original schema in §Next Steps step #2)

| Field | Type | Required | Notes |
|---|---|---|---|
| Builders | Linked records → Builders | — | Multi-link. Order matters for display sequence. |

*(No direct shop link on Parks — Nearby Shops is computed at build time from lat/lng.)*

See [SITE-AUDIT.md §7](SITE-AUDIT.md) for the full audit-informed Parks schema expansion (square footage, year established, hours, status, etc.) — adds ~8 more fields beyond the original 14 based on what's already in the live data.

#### Resolved Open Questions
- ~~**#5 Airtable vs. existing CMS migration**~~ — Current site is WordPress with custom post types `park`, `builder`, `shop`. Import via WP REST API or DB export. See SITE-AUDIT.md §1.
- ~~**#6 Embeddable widget**~~ — Confirmed deferred.

#### Refined Open Questions
- **#1 URL structure** → Resolved for parks: keep `/park/<slug>/` verbatim. Resolved for builders/shops: 410 Gone for the 34 URLs. Still open: should taxonomy archives (`/regions_and_counties/<slug>/`, `/park_obstacles/<slug>/`) migrate as Next.js routes? See SITE-AUDIT.md §6.

#### New Open Questions surfaced by the audit
See SITE-AUDIT.md §9 for the full list. The most consequential:
1. **Park position counter ("1/12", "1/14") on profile pages** — what does this number mean on the live site? If editorial, must migrate.
2. **Lat/lng extraction** — coordinates live in WP Google Maps plugin tables; need DB export, not HTML scraping.
3. **`/style-guide/` page** — owner may already have an established palette/type system. Read before final visual design.
4. **Status field for parks** — live site tracks Open / Temporarily Closed / Permanently Closed. New Airtable schema should include this.
5. **Park Type vs Riding Surface** — live site has both as separate fields. Original schema collapsed them into `Material`. Should be separated.
6. **38 obstacle terms** — original schema had 5 Style values. Live site has 38 obstacle taxonomy terms with per-park tagging. Recommend migrating all 38.

### 2026-05-30 — Mockup field walkthrough (D9–D28)

After reviewing the mobile mockup ([mobile-design/](mobile-design/)) field by field. Twenty structural decisions. Full Parks-table schema below; this is now the source of truth for park-page fields.

#### Decisions summary

| # | Section | Decision |
|---|---|---|
| D9 | Hero contents | Photo, city/state, name, established year only. Park type and sqft are NOT in the hero. |
| D10 | Park Type / Sqft / County | Dropped from public profile page. Stay in Airtable for filter facets on the parks index only. |
| D11 | Status | Silent when Open. Banner when Temporarily or Permanently Closed. **Page stays live for Perm Closed** (historical record). |
| D12 | Address | Structured: `StreetAddress` + `City` + `State` + `ZIP` + `Lat` + `Lng`. Street can be approximate or blank (FDR-style intersections). |
| D13 | Hours | Single long-text field. Line breaks render as stanzas. Handles seasonal splits, dawn-to-dusk, day-of-week rules uniformly. |
| D14 | Overview photos | Single `Photos` field. Inline: horizontally scrollable thumbnail strip (1–3+ visible). Lightbox on tap shows all. |
| D15 | Vehicle types | 4 checkboxes (`AllowsSkateboards` / `AllowsBikes` / `AllowsRollerSkates` / `AllowsScooters`, default true) + `VehicleRulesNotes` long text. Helmets explicitly out of this — they're in Pads. |
| D16 | Pads / Helmets | `Helmets` single-select (None posted / Recommended / Required under 12 / Required all ages) + `OtherPadsRequired` checkbox. Mockup row renders dynamically from these. |
| D17 | Fee | Single boolean. No cost detail field. Paid parks just show "Fee: Yes". |
| D18 | Amenity model | Universal `Y/N + Notes + Photo` for all 7 amenity rows. 21 fields total. Renderer hides empty notes/photos. |
| D19 | Amenity typology | Dropped entirely. Owner types `porta-potty` or `lot parking` as free text in the per-amenity Notes field. |
| D20 | Riding Surface | Multi-select (`Concrete` / `Asphalt` / `Wood` / `Other`) + Notes + Photo. The one place typology survives because riding surface IS typology. |
| D21 | Connect link shape | Single multi-line `ParkLinks` field, format `Type \| URL` per line. Renderer maps Type to platform-specific button styling. |
| D22 | Operator | Not imported at all. Field deleted; data lost (Bellevue Borough etc.). |
| D23 | Support the Park | Folded into `ParkLinks`. Same convention. Renderer segments Connect-section vs Support-section by Type. |
| D24 | Nearby Parks | Kept at bottom of profile. 3 closest within 30mi, Haversine compute, visual style mirrors Nearby Shops. Zero new fields. |
| D25 | Obstacles | Chip/tag list on profile. Multi-select with 38 options. Section appears between Riding Surface and Programming. |
| D26 | Builders placement | Small standalone "Built by" section near the bottom, just before Nearby Parks. Attribution-style. |
| D27 | Programming module | **Renamed from "Coaching" to "Programming"** (parent-friendly word). Mid-page section, renders when `Programming = true`. |
| D28 | Suggest an Edit | Modal on park page → Airtable Suggestions table (linked back to park). Owner triages in Airtable. |
| D29 | Photo credits | `Photos` becomes a child table (`ParkRef` + `File` + `Credit` + `Caption` + `Order`). Replaces the multi-attachment field on Parks. Forward-compatible for contributor credits. |
| D30 | Renovated dates | Multi-line `RenovatedYears` text field. One year per line, optional `YYYY: notes` format. Renders alongside the EST. YYYY pill in the hero ("Renovated 2017, 2022"). |

#### Full Parks-table schema

##### Identity & URL
| Field | Type | Required |
|---|---|---|
| `Name` | Single line text | ✅ |
| `Slug` | Single line text (unique) | ✅ |
| `Status` | Single select: Open / Temporarily Closed / Permanently Closed | ✅ |

##### Hero (renders on page)
| Field | Type | Required |
|---|---|---|
| `City` | Single line text | ✅ |
| `State` | Single select (default PA) | ✅ |
| `EstablishedYear` | Number | — |
| `RenovatedYears` | Long text (one year per line, optional `YYYY: notes`) | — |

*Hero photo = `Photos[0]`. No separate hero-photo field. EST. YYYY pill extends to show "Renovated 2017, 2022" when `RenovatedYears` is populated. Notes after a colon render as a tooltip or inline detail.*

##### Indexed but not rendered on profile (filter facets only)
| Field | Type | Required |
|---|---|---|
| `ParkType` | Single select: Concrete Park / DIY Park / Indoor Park / Prefab Park / Skate Plaza | — |
| `SquareFootage` | Number | — |
| `County` | Single select | — |

##### Address
| Field | Type | Required |
|---|---|---|
| `StreetAddress` | Single line text | — |
| `ZIP` | Single line text | — |
| `Lat` | Number | ✅ |
| `Lng` | Number | ✅ |

##### Hours
| Field | Type | Required |
|---|---|---|
| `Hours` | Long text (line-break stanzas) | — |

##### Overview
| Field | Type | Required |
|---|---|---|
| `Description` | Long text | — |
| `Photos` | → Photos child table (see D29 / Resolved section below) | — |

##### Park Rules
| Field | Type | Required |
|---|---|---|
| `AllowsSkateboards` | Checkbox (default true) | — |
| `AllowsBikes` | Checkbox (default true) | — |
| `AllowsRollerSkates` | Checkbox (default true) | — |
| `AllowsScooters` | Checkbox (default true) | — |
| `VehicleRulesNotes` | Long text | — |
| `Helmets` | Single select: None posted / Recommended / Required under 12 / Required all ages | — |
| `OtherPadsRequired` | Checkbox | — |
| `Fee` | Checkbox | — |

##### Amenities (universal Y/N + Notes + Photo — 7 rows × 3 fields = 21 fields)
| Amenity | Fields |
|---|---|
| Bathroom | `BathroomYN`, `BathroomNotes`, `BathroomPhoto` |
| Drinking Water | `DrinkingWaterYN`, `DrinkingWaterNotes`, `DrinkingWaterPhoto` |
| Lights | `LightsYN`, `LightsNotes`, `LightsPhoto` |
| Parking | `ParkingYN`, `ParkingNotes`, `ParkingPhoto` |
| Spectator Area | `SpectatorAreaYN`, `SpectatorAreaNotes`, `SpectatorAreaPhoto` |
| Onsite Shop | `OnsiteShopYN`, `OnsiteShopNotes`, `OnsiteShopPhoto` |
| Equipment Rentals | `EquipmentRentalsYN`, `EquipmentRentalsNotes`, `EquipmentRentalsPhoto` |

##### Riding Surface
| Field | Type | Required |
|---|---|---|
| `RidingSurface` | Multi-select: Concrete / Asphalt / Wood / Other | — |
| `RidingSurfaceNotes` | Long text | — |
| `RidingSurfacePhoto` | Attachment (single) | — |

##### Obstacles
| Field | Type | Required |
|---|---|---|
| `Obstacles` | Multi-select (38 options from live site taxonomy) | — |

##### Programming module
| Field | Type | Required |
|---|---|---|
| `Programming` | Checkbox | — |

*When true, renders the Programming section mid-page. CTAs are template-level, not per-park.*

##### Connect + Support
| Field | Type | Required |
|---|---|---|
| `ParkLinks` | Long text. Each line: `Type \| URL` | — |

Recognized Types:
- **Connect section:** `Website`, `Instagram`, `Facebook`, `Twitter`, `YouTube`, `TikTok`
- **Support section:** `GoFundMe`, `Venmo`, `Patreon`, `Donate`, `GiveButter`, `PayPal`
- **Other Links section** (fallback): anything not recognized above

##### Builders (relational)
| Field | Type | Required |
|---|---|---|
| `Builders` | Linked records → Builders table (multi) | — |

#### Visual order on the profile (top to bottom)

1. Hero (photo + city/state + name + EST year)
2. **Status banner** (only when not Open)
3. Address
4. Hours
5. Overview (description + horizontally-scrollable thumbnail strip)
6. Park Rules (vehicles + pads + fee)
7. Amenities (7 rows)
8. Riding Surface
9. **Programming module** (only when `Programming = true`)
10. Obstacles (chip list)
11. Builders ("Built by" attribution)
12. Connect (rendered from `ParkLinks`)
13. Support (rendered from `ParkLinks`)
14. Suggest an Edit (button → modal)
15. Nearby Parks (3 closest, Haversine, 30mi cap)
16. Nearby Shops (3 closest, Haversine, 30mi cap, no state border rule)

#### Field totals

| Table | Field count |
|---|---|
| Parks | ~52 (`Photos` moved to child table per D29; `RenovatedYears` added per D30) |
| Builders | 3 (`Name`, `Link`, `Logo`) |
| Shops | 7 (`Name`, `Link`, `Logo`, `Address`, `Lat`, `Lng`, `State`) |
| Suggestions | 7 (`ParkRef`, `Name`, `Email`, `ChangeDescription`, `Reason`, `Status`, `CreatedAt`) |
| Photos | 5 (`ParkRef`, `File`, `Credit`, `Caption`, `Order`) |

#### Renames / removals vs the original DESIGN.md schema

| Change | Why |
|---|---|
| ~~`Material`~~ → `RidingSurface` | Matches live-site label; clearer |
| ~~`Style`~~ → dropped | Replaced by `Obstacles` multi-select (38 terms) |
| ~~`Lighting` checkbox~~ → `LightsYN` (+ Notes + Photo) | Universal amenity model |
| ~~`Helmets` single-select~~ → kept as is, but standalone (no `PadsRequired` boolean) | Pads model is now Helmets + OtherPadsRequired |
| ~~`Beginner` checkbox~~ → dropped | Not in mockup; not surfaced in audit data |
| ~~`InstagramHandle` (single)~~ → folded into `ParkLinks` | Mockup needs multi-handle support |
| ~~`Operator`~~ → not imported | D22: data not migrated |
| ~~`FundraiserUrl`~~ → folded into `ParkLinks` | D23: unified link convention |
| ~~`Coaching`~~ → `Programming` | D27: parent-friendly rename |
| ~~`WordPressPostId`~~ → not needed | Production schema has no WP linkage |

#### New tables introduced this session

- **Suggestions** — captures user-submitted corrections from the Suggest an Edit modal. Linked back to Parks. Status pipeline: New → In Review → Applied / Rejected.

#### Resolved 2026-05-30 (post-mockup investigation)

- ✅ **Position counter ("1/12", "1/14")** — NOT a data field. It's the photo gallery UI counter (HTML class `image-gallery-count`). "1/12" = "viewing image 1 of 12 in this park's gallery." The new lightbox renders the same counter from photo array length. Zero migration impact.
- ✅ **`/style-guide/` page** — irrelevant. Per owner clarification 2026-05-30, the page is old placeholder content AND the live site's design system is NOT canonical for the rebuild. Nothing to extract. The new design direction will be defined separately (likely via `/design-consultation` in a future session).
- ✅ **Photo permissions / credits** — live WP site has zero per-photo metadata (sampled 20/20 photos: no alt text, no captions, no credit language). Nothing to migrate. D29 introduces a Photos child table for forward contributor credits.

#### D29 — Photos child table (replaces the single Photos attachment field on Parks)

| Field | Type | Required |
|---|---|---|
| `ParkRef` | Linked records → Parks | ✅ |
| `File` | Attachment (single) | ✅ |
| `Credit` | Single line text | — |
| `Caption` | Long text | — |
| `Order` | Number | — |

Implications:
- The Parks `Photos` field becomes a relation; renderer queries Photos by `ParkRef`, sorts by `Order`.
- **Amenity photos** (`BathroomPhoto`, etc.) stay as simple attachments on Parks — owner-sourced, no contributor credit needed.
- **Builder and Shop logos** stay as simple attachments — single logo per record.
- **Accessibility:** alt text auto-generates from park name + photo index when `Credit`/`Caption` are absent (e.g., `"FDR Skatepark photo 3"`). Owner-supplied `Caption` overrides; `Credit` appears as a small attribution below the photo in the lightbox.

#### Deferred to future iterations

- **Park rating system (1–10 score)** — discussed and explicitly parked 2026-05-30. v1 ships as an impartial directory (Wikipedia-style). Future iterations may introduce an owner score + community reviews + computed-signal blend. Architecture options analyzed in session conversation: (A) single owner score + computed signals as separate filters + future community reviews table; (B) computed composite from existing signals; (C) multi-dimensional scores (parent/skater/overall). No fields added to v1 schema for rating. If revisited: candidate fields include `OwnerScore`, `OwnerScoreNotes`, `OwnerScoreDate`, `Condition`, `SkillLevel`, `Notable`, plus a `Reviews` child table.

---

## Problem Statement

Pennsylvania has 150+ public skateparks. Most people — including people who live within 10 minutes of one — don't know they exist. The goal of this project is to make skateparks accessible through awareness.

The site serves three audiences:
1. **Parents** (primary) — looking for activities for their kids who are interested in skateboarding. They don't know what skateparks look like, whether they're safe, or whether there's one near them. The site must be welcoming to non-skaters.
2. **Core skaters** (secondary) — people who travel around to different parks, want a complete and detailed state-wide directory.
3. **Aspiring coaches / skate educators** (tertiary) — skaters or educators interested in becoming skateboarding coaches. Reached through a conditional coaching module on park profiles.

The site owner also runs a skateboarding coaching business. The directory is a trust-building resource that funnels parents to coaching programs and recruits future coaches.

## What Makes This Cool

The data is the moat. The owner spent months compiling a list of 150+ PA parks — nearly double the listings of the next largest resource. National apps (Smap, Ramp Map, Concrete Disciples) are built for skaters who already know what they want. This site flips that assumption: it's built for people who don't know options exist near them.

The "whoa" moment for a parent: they search "skateparks near Pittsburgh PA" and discover a free, public park 8 minutes from their house that their kid would love — with plain-English descriptions, a photo, helmet requirements, and one-tap directions.

## Constraints

- Rebuild of existing live site at paskateparks.com — no green-field, existing SEO and URL structure to consider
- Data management must be non-technical: 150+ parks need to be updated without code changes
- Primary device is mobile — a parent checking from their phone in a parking lot or at home
- Tone: simple, informative, welcoming to non-skaters. No skate jargon in UI copy.

## Premises

1. **The data is the moat.** 150+ parks, most complete PA listing. The design must showcase and protect it.
2. **This is a rebuild**, not net-new. paskateparks.com is already live.
3. **Serve both discovery and search.** Discovery: "I didn't know there was a park near me." Search: "I want to find a good park in Philadelphia." Both entry points matter — neither dominates.
4. **Tone welcoming to non-skaters.** If a parent can't figure it out in 10 seconds, it failed.

## Cross-Model Perspective

Second opinion (Claude subagent, builder mode):

> The coolest version not yet considered: a proximity engine — open the site, share location, see the nearest park in 3 seconds with a photo, whether it's lit, whether it has a bathroom, one-tap directions. No account, no friction. Plus a printable "first visit" card per park (what to expect, what to bring, beginner-friendly?). Embeddable widget for local news sites, tourism boards, school districts — "Skateparks near [town name]." That's free distribution without SEO.

> The one line that reveals what drives this: "nearly double the listings of the next largest resource." That's not a feature — it's an identity. They're not building a website, they're asserting that they are THE authoritative source on PA skateparks.

> 50% solution: Leaflet.js (free interactive maps, OpenStreetMap tiles, no API key) + Airtable (spreadsheet CMS for 150+ parks, read-only API). The 50% to build: park profile pages, proximity/geolocation feature, photo hosting, parent-friendly UX, SEO per park.

> Critical insight: server-side render the park data — don't client-side fetch it. If the map spins 4 seconds on an Android in a parking lot, the premise fails.

## Approaches Considered

### Approach A: Static-First (Ship This Week)
- JSON data file + Leaflet map + plain HTML
- Deploy to Netlify or GitHub Pages in an afternoon
- **Effort:** S (human: 2-3 days / CC: ~1 hour)
- **Risk:** Low
- **Pros:** Ships fastest. Zero infrastructure. Easy to hand off or open source.
- **Cons:** No individual park pages — bad for SEO. Updating data means editing JSON. Hard to layer in photos/filters later.

### Approach B: Next.js + Airtable + Leaflet
- App Router Next.js with static generation (ISR) per park — strong SEO, fast cold loads
- Airtable as spreadsheet-style CMS for park data management
- Leaflet map with OpenStreetMap tiles (Leaflet must be dynamically imported with `{ ssr: false }` — it requires `window` and will throw during SSR)
- Vercel deploy
- **Effort:** M (human: 1-2 weeks / CC: ~3-4 hours)
- **Risk:** Low
- **Pros:** Each park gets its own URL that ranks in Google for local searches. Data updated from Airtable — no code required. Static generation means fast load on mobile. Clean path to add photos, filters, profiles over time.
- **Cons:** More setup than Approach A. Airtable free tier has API rate limits — mitigated by fetching all parks in a single paginated list call at build time (not per-page fetches, which would hit the 5 req/sec limit).

### Approach C: Proximity-First PWA
- Progressive Web App with geolocation as the homepage
- Offline capable. Embeddable widget for school/township sites.
- **Effort:** L (human: 3-4 weeks / CC: ~6-8 hours)
- **Risk:** Med — PWA install prompts are inconsistent across iOS/Android. Widget maintenance adds scope.
- **Pros:** The parent experience is genuinely surprising — no searching required, just location. Organic distribution through embeds.
- **Cons:** Longest path to something shareable. More complexity than the problem requires at this stage.

## Recommended Approach

**Approach B: Next.js + Airtable + Leaflet + Vercel.**

SEO is non-negotiable — parents searching "skateparks near [city] PA" need to find individual park pages, not just a homepage. Airtable as the CMS keeps data management sustainable (update from a spreadsheet, no code edits). Static generation (ISR) ensures fast mobile load without per-request server overhead.

The "Near Me" geolocation button is in scope for phase 1 — it's a single client-side `navigator.geolocation` call that re-centers the map and sorts the park list. It doesn't change the architecture. Fallback when the user denies location permission: prompt manual city or zip code entry.

## UI Direction

Wireframe: `/tmp/paskateparks-wireframe.html` (session artifact — open in a browser to view; key decisions captured in this doc)

**Three screens designed:**

**Homepage:** Full-screen Leaflet map + sidebar park list. Hero copy above the map: "150+ skateparks across Pennsylvania. Find one near you." Search bar + "Near Me" button. Nav: All Parks / For Parents / About.

**Park Profile (standard):** Full-width single-column layout. Gallery (1/2/3 photo grid, click opens modal lightbox, "🔍 View all N photos" badge signals interactivity) → address/directions inline → features grid (material-first: Concrete / Wood / Asphalt, then Lighting, Helmets, Beginner ok, Cost) → plain-English description → nearby parks (3-column card grid).

**Park Profile (coaching-enabled):** Same layout with a narrow sidebar on the right containing a coaching module. The sidebar renders only when `Coaching` (boolean field) is set to `true` in Airtable — a manual flag the site owner sets per park. All other parks: no sidebar, full-width content. Coaching module has two CTAs: "Find a Coach →" (for parents) and "Skater or educator? Learn about becoming a coach" (for recruitment).

**Nearby parks algorithm:** 3 closest parks by Haversine distance within 30 miles of the current park. Computed at build time from the Airtable lat/lng fields and stored in the static page props. If fewer than 3 parks exist within 30 miles, show however many do.

**Missing data fallbacks:**
- No photos: render a styled placeholder (park name + city on a gray background, no broken image)
- No description: omit the "About This Park" section silently
- Missing lat/lng: exclude from the map; log a warning at build time so the owner knows which parks need geocoding
- No Coaching flag: default to `false` — full-width layout, no sidebar

**Mobile:** Hamburger menu preserves nav items. Search field + "Near Me" button in the body above the map. Park selection opens a bottom sheet containing: park name, distance from user location (or city), material tags, and two CTAs — "Get Directions" (opens native maps app) and "View Park Details →" (navigates to the full profile page).

**Gallery layout rules:**
- 1 photo: single image, full width, 240px tall
- 2 photos: side-by-side, equal width, 200px tall
- 3 photos: large image on left spanning full height (2fr), two stacked images on the right (1fr each)
- Clicking any image opens a modal lightbox — recommend `yet-another-react-lightbox` (small, accessible, maintained). "🔍 View all N photos" badge overlaid on the bottom-right corner of the gallery.

**Printable "first visit" card:** Out of scope for this rebuild. Revisit if the coaching business has a use for it.

**Tag design:** Material-specific tags surfaced on park cards and profiles (Concrete, Wood ramps, Asphalt, Bowl, Street section) — not generic "Free" / "Outdoor" which apply to 90%+ of parks. Parent-specific info (Beginner ok, Helmets required, Lit at night) surfaced alongside material tags.

**"New to Skateparks" block:** Lives on For Parents and About pages only — not on park profiles.

## Open Questions

1. **URL structure + SEO migration:** Does the existing paskateparks.com use URL slugs for parks? If yes, they must be preserved in the Next.js rebuild to protect existing search rankings. If URLs change, add 301 redirects from old to new paths in `next.config.js`. Also needed at launch: `robots.txt` allowing all crawlers, `sitemap.xml` listing all park URLs, and `<link rel="canonical">` on every park page.
2. **Photo hosting:** Where do park photos live? Airtable attachments field works at low volume but has size limits. Consider Cloudinary or an S3 bucket as photo scale increases.
3. **Geocoding:** Are all 150+ parks already geocoded (lat/lng)? If not, a one-time geocoding pass (Google Geocoding API or Nominatim) is needed before the map can render them.
4. ~~**Coaching flag:**~~ Resolved — manual flag (`Coaching` boolean) set by site owner per park in Airtable. Default: false.
5. **Airtable vs. existing CMS:** Does the current paskateparks.com already have a data store (WordPress, Webflow, spreadsheet)? The import path from existing data to Airtable needs to be planned.
6. **Embeddable widget:** Out of scope. Design in a future session if the need is validated.

## Success Criteria

- All 150+ parks visible on the map at launch
- Individual park pages rank in Google for "[city] skatepark Pennsylvania" within 90 days
- A parent can find their nearest park and get directions in under 30 seconds on mobile
- Park data can be updated in Airtable without a code deploy
- Coaching module visible and functional on specified parks

## Distribution Plan

- **Web service** hosted on Vercel (auto-deploy on merge to main)
- **Domain:** paskateparks.com (existing, transfer DNS to Vercel)
- **Data pipeline:** Airtable → Next.js on-demand ISR via Vercel deploy hook. All 150+ parks fetched in a single paginated list call at build time (not per-page fetches). Revalidation model: on-demand ISR using `revalidatePath` triggered by a Vercel deploy hook URL — Airtable automation POSTs to `https://api.vercel.com/v1/integrations/deploy/<hook-id>` on every record save. Time-based fallback: `revalidate: 3600` (1 hour). Changes go live within minutes of an Airtable save.
- **Airtable API key:** Stored as a Vercel environment variable (`AIRTABLE_API_KEY`). Never exposed client-side — all Airtable fetches happen at build time in `generateStaticParams` / `generateMetadata`, not in client components.
- **Airtable record ceiling:** Free tier allows 1,000 records per base. At 150 parks this is fine. If the scope ever expands beyond PA, migrate to a paid tier before hitting the limit to avoid silent data truncation at build time.
- **CI/CD:** Vercel's built-in GitHub integration — push to main triggers deploy

## Next Steps

1. ✅ **Audit existing paskateparks.com** — completed 2026-05-30. Results in [SITE-AUDIT.md](SITE-AUDIT.md). Key findings: WordPress with 3 custom post types (`park`, `builder`, `shop`), `/park/<slug>/` URL pattern, 47 of 146 parks fully built out (the other 99 represent content debt, not a different tier), 38 obstacle taxonomy terms, 8 schema fields not in the original design.
2. **Import park data to Airtable** — create a base with the following schema, then import all 150+ parks. **Note: this schema is extended by the audit — see Revisions section above and [SITE-AUDIT.md §7](SITE-AUDIT.md) for the augmented schema before importing.**

| Field | Type | Required | Notes |
|---|---|---|---|
| Name | Single line text | ✅ | Display name |
| Slug | Single line text | ✅ | URL slug, e.g. `allentown-skate-plaza`. Unique. |
| City | Single line text | ✅ | |
| County | Single line text | ✅ | |
| Address | Single line text | ✅ | Full street address |
| Lat | Number | ✅ | Decimal degrees |
| Lng | Number | ✅ | Decimal degrees |
| Material | Single select | ✅ | Allowed: `Concrete`, `Asphalt`, `Wood`, `Mixed` |
| Style | Single select | — | Allowed: `Street plaza`, `Bowl`, `Street + bowl`, `Pump track`, `Mixed` |
| Lighting | Checkbox | — | True if lit at night |
| Helmets | Single select | — | Allowed: `Required under 12`, `Required all ages`, `Recommended`, `None posted` |
| Beginner | Checkbox | — | True if suitable for beginners |
| Description | Long text | — | Plain English, no jargon |
| Photos | Attachments | — | Up to 3 images. Fallback to placeholder if empty. |
| Coaching | Checkbox | — | True = render coaching sidebar. Default: false. |
3. **Geocode any missing parks** — run any parks without lat/lng through Nominatim or Google Geocoding API.
4. **Scaffold Next.js app** — App Router, Tailwind, Leaflet client component, Airtable API integration. Deploy empty shell to Vercel.
5. **Build map page** — full-screen Leaflet map, pins from Airtable data, click opens park sidebar list entry.
6. **Build park profile pages** — static generation from Airtable, gallery component, directions block, features grid, nearby parks, coaching sidebar (conditional).
7. **Mobile polish + SEO** — page titles, meta descriptions, Open Graph tags per park, sitemap.xml.

## What I noticed about how you think

- You corrected the "awareness vs. search" premise immediately and precisely: *"all of these options are true"* — you didn't pick a side, you identified that the framing was a false choice. That kind of precision matters when you're writing product specs.
- When describing the audience, you went specific fast: *"parents who are looking for a way to help their kids who are interested in skateboarding."* That's a person, not a category. Most people stay at "families."
- You removed things. The "By County" nav item, the generic tags, the oversized sidebar — you knew what was redundant without being asked twice. Subtraction is harder than addition.
- The coaching angle wasn't in the original pitch. You added it mid-session, and you added it correctly — not as a nav item, not as a homepage takeover, but as a contextual sidebar on the right parks. You already understood where in the funnel it belongs.
